---

---

<div class="left-ad">
    <div class="left-ad-container">
        <div class="left-ad-content">
            <div class="left-ad-title">
                <span class="lambda-icon">λ</span> TIME-LIFE 3
            </div>
        </div>
    </div>
</div>

<style>
    .left-ad {
        position: fixed;
        left: 0;
        top: 50%;
        transform: translateY(-50%);
        height: auto;
        background: rgba(10, 6, 0, 0.4);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid rgba(212, 120, 0, 0.2);
        display: flex;
        align-items: center;
        padding: 0 2rem;
        z-index: 10;
    }

    .left-ad-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
    }

    .left-ad-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 0 2rem;
    }

    .left-ad-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--hl-primary);
        text-decoration: none;
        letter-spacing: 4px;
        text-shadow: 0 0 10px rgba(212, 120, 0, 0.4);
        transition: all 0.3s ease;
    }

    .left-ad-title:hover {
        color: var(--hl-glow);
        text-shadow: 0 0 15px rgba(255, 180, 0, 0.6);
    }

    .left-ad-title:hover .lambda-icon {
        transform: scale(1.1);
        filter: brightness(1.2);
    }

    .left-ad-right {
        display: flex;
        gap: 2.5rem;
    }

    .left-ad-link {
        color: var(--hl-accent);
        text-decoration: none;
        text-transform: uppercase;
        font-size: 0.8rem;
        font-weight: 700;
        letter-spacing: 2px;
        opacity: 0.7;
        transition: all 0.3s ease;
        position: relative;
    }

    .left-ad-link::after {
        content: "";
        position: absolute;
        bottom: -5px;
        left: 0;
        width: 0;
        height: 2px;
        background: var(--hl-primary);
        transition: width 0.3s ease;
        box-shadow: 0 0 5px var(--hl-primary);
    }

    .left-ad-link:hover {
        opacity: 1;
        color: var(--hl-primary);
    }

    .left-ad-link:hover::after {
        width: 100%;
    }

    @media (max-width: 768px) {
        .left-ad {
            position: relative;
            top: 0;
            transform: none;
            width: 100%;
            padding: 1rem;
            justify-content: center;
            border-bottom: 1px solid rgba(212, 120, 0, 0.2);
            margin-top: 70px;
        }
        .left-ad-container {
            justify-content: center;
        }
        .left-ad-right {
            gap: 1.5rem;
        }
        .left-ad-title {
            font-size: 1.2rem;
            letter-spacing: 2px;
        }
        .left-ad-link {
            font-size: 0.7rem;
            letter-spacing: 1px;
        }
    }
</style>
